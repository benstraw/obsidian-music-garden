# Public contract v1 schemas

These Draft 2020-12 schemas describe the stable v1 envelope. Minor v1
releases may add fields. Consumers must validate the manifest, verify every
listed SHA-256 digest, reject unsupported major versions, and treat unknown
entity fields as forward-compatible additions.
